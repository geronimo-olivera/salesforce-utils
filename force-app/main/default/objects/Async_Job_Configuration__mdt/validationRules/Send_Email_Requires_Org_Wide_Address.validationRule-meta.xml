<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Send_Email_Requires_Org_Wide_Address</fullName>
    <active>true</active>
    <description>An Organization-Wide Email Address Id must be set whenever Send_Email_On_Failure__c is checked — sending without one can fail depending on the running user's own email context.</description>
    <errorConditionFormula>Send_Email_On_Failure__c &amp;&amp; ISBLANK(Org_Wide_Email_Address_Id__c)</errorConditionFormula>
    <errorDisplayField>Org_Wide_Email_Address_Id__c</errorDisplayField>
    <errorMessage>Org Wide Email Address Id is required when Send Email On Failure is checked.</errorMessage>
</ValidationRule>
